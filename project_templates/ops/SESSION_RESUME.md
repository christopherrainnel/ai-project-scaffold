# Session Resume

Before anything else this session:
1. Use the current working-tree governance files, not older committed or remote copies.
2. Read `AGENTS.md`, then `docs/FILE_MAP.md`, then `ops/AI_WORKFLOW.md`.
3. Run a workstation context check: repo root, OS/shell/runtime, local environment health, git hooks, and any path-sensitive assumptions.
4. If the task touches entitlement boundaries and a local tiering doc exists, read it.
5. Open only the files needed for this task.
6. If resuming, read only the newest relevant `CHANGELOG_AI.md` entry.

If required human practical testing remains after AI-runnable verification, mark the work `Awaiting human validation` and pause for the user's result.
